{% extends ['page.html.twig', '_default/page.html.twig'] %}

              {%- block content ~%}
              {{ page.content }}
              {%~ include 'partials/subscribe.html.twig' ~%}
              {%- endblock ~%}